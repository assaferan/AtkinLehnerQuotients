.DS_Store

# Generated by the driver scripts in scripts/ (gen_genus_models.m,
# pointsearch_*.m, run_triple_covers.m, classify_triple_covers.m and
# run_pointsearch_g8.sh all write here).  Nothing under outputs/ is tracked.
# Note this is NOT the same as tests/logs/, whose transcripts are committed
# deliberately as reference output.
outputs/

# Local agent/tool state, not part of the project.
.claude/
